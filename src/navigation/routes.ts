export enum Stacks {
  PUBLIC = 'STACK_PUBLIC',
  PRIVATE = 'STACK_PRIVATE',
  MODAL = 'STACK_MODAL',
}

export enum Routes {
  // PUBLIC ROUTES
  LOGIN = 'LOGIN',
  // MODAL ROUTES
  INFO = 'INFO',
  // PRIVATE ROUTES
  HOME = 'HOME',
}
