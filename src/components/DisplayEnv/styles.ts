import styled from 'styled-components/native';
import { Typography } from '~/components';

export const Text = styled(Typography).attrs({ variant: 'subhead' })`
  text-align: center;
  color: red;
  margin-vertical: 15px;
`;
